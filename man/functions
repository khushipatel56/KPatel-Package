\name{ScriptOverview}
\alias{ScriptOverview}
\title{Overview of R Scripts: Major Steps}
\description{
  This document outlines the major steps taken by each R function in the provided scripts.
}
\section{1. \code{association_rules.R} - \strong{Generate Association Rules}}{
  \enumerate{
    \item Coerce input data into a \code{transactions} object.
    \item Apply the Apriori algorithm using \code{arules::apriori()} with specified support and confidence.
    \item Return the generated association rules.
  }
}
\section{2. \code{categorize_data.R} - \strong{Categorize Data Column}}{
  \enumerate{
    \item Create a new column name by appending \code{"\_category"} to the original column name.
    \item Use \code{cut()} to divide the numeric column into bins with labels.
    \item Add the categorized column to the data frame.
    \item Return the modified data frame.
  }
}
\section{3. \code{eda_summary.R} - \strong{Exploratory Data Summary}}{
  \enumerate{
    \item Compute summary statistics of the data using \code{summary()}.
    \item Capture structure information using \code{str()} and \code{capture.output()}.
    \item Return both summary and structure in a list.
  }
}
\section{4. \code{test_hypothesis.R} - \strong{Hypothesis Testing}}{
  \enumerate{
    \item Check which test type is specified: \code{"t.test"} or \code{"chisq.test"}.
    \item If \code{"t.test"}: Perform t-test between the two numeric variables.
    \item If \code{"chisq.test"}: Create a contingency table and run chi-square test.
    \item Return the result of the selected test.
    \item Throw an error if an unsupported test type is given.
  }
}
\section{5. \code{train_model.R} - \strong{Train Predictive Model}}{
  \enumerate{
    \item Call \code{caret::train()} using the specified formula, data, method, and optional tuning grid.
    \item Return the trained model object.
  }
}
\section{6. \code{visualize_data.R} - \strong{Visualize Data}}{
  \enumerate{
    \item Depending on the specified \code{type}, choose the correct \code{ggplot2} geom:
      \itemize{
        \item \code{"scatter"} \verb{->} \code{geom_point()}
        \item \code{"bar"} \verb{->} \code{geom_bar()}
        \item \code{"boxplot"} \verb{->} \code{geom_boxplot()}
      }
    \item Apply aesthetics using \code{aes_string()} to handle variable inputs.
    \item Add minimal theming with \code{theme_minimal()}.
    \item Return the constructed ggplot object.
    \item Throw an error for unsupported plot types.
  }
}
\keyword{documentation}
